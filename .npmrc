# Some plugins declare Capacitor 8 peers while the app uses Capacitor 7.
# Keep installs reproducible until the native dependency versions are aligned.
# Native compatibility must be verified on a device.
legacy-peer-deps=true
