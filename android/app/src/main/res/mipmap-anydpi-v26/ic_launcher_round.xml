<?xml version="1.0" encoding="utf-8"?>
<adaptive-icon xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- jacksonicon.jpg fills the entire 108dp canvas (including bleed area) -->
    <background android:drawable="@drawable/ic_launcher_bg_image"/>
    <!-- Transparent foreground so the full background image is visible -->
    <foreground android:drawable="@android:color/transparent"/>
</adaptive-icon>
